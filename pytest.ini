# pytest.ini
[pytest]
minversion = 6.0
addopts = -ra -q
testpaths =
    backend/tests
python_files = test_*.py
asyncio_mode = strict

# Para evitar el warning de scope
filterwarnings =
    ignore::pydantic.PydanticDeprecatedSince20
    ignore::pytest.PytestDeprecationWarning